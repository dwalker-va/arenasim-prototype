{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then echo '📝 Reminder: Update design-docs/project-todos.md with this commit (session notes, files modified, key learnings)'; fi"
          }
        ]
      }
    ]
  }
}
