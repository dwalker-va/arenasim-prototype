diff --git a/src/states/play_match/combat_core/auto_attack.rs b/src/states/play_match/combat_core/auto_attack.rs
index 0933aec..28311c8 100644
--- a/src/states/play_match/combat_core/auto_attack.rs
+++ b/src/states/play_match/combat_core/auto_attack.rs
@@ -77,22 +77,42 @@ pub fn combat_auto_attack(
     // (pet.slot - PET_SLOT_BASE) so a pet lines up with its owner's number.
     let combatant_info: std::collections::HashMap<
         Entity,
-        (u8, match_config::CharacterClass, String, bool, bool, u8),
+        (
+            u8,
+            match_config::CharacterClass,
+            String,
+            bool,
+            bool,
+            u8,
+            AutoAttackKind,
+        ),
     > = combatants
         .iter()
         .map(|(entity, _, combatant, _, _, _)| {
-            let (display_name, is_melee, slot_label) =
+            let (display_name, is_melee, slot_label, kind) =
                 if let Ok(pet) = auto_attack_pet_query.get(entity) {
+                    // Pets carry no equipment. A pet's auto-attack is its own
+                    // pet type's, and the pre-AS-99 ladder read the OWNER's
+                    // class for the ranged arm — reproduce that exactly.
+                    let melee = pet.pet_type.is_melee();
                     (
                         pet.pet_type.name().to_string(),
-                        pet.pet_type.is_melee(),
+                        melee,
                         combatant.owner_relative_slot(),
+                        if melee {
+                            AutoAttackKind::Melee
+                        } else if combatant.class == match_config::CharacterClass::Hunter {
+                            AutoAttackKind::Shot
+                        } else {
+                            AutoAttackKind::Wand
+                        },
                     )
                 } else {
                     (
                         combatant.class.name().to_string(),
                         combatant.class.is_melee(),
                         combatant.slot,
+                        combatant.auto_attack_kind,
                     )
                 };
             (
@@ -104,6 +124,7 @@ pub fn combat_auto_attack(
                     is_melee,
                     combatant.is_alive(),
                     slot_label,
+                    kind,
                 ),
             )
         })
@@ -271,14 +292,16 @@ pub fn combat_auto_attack(
 
                     // Use pet-aware is_melee from snapshot (pets inherit owner's class
                     // but may have different melee/ranged behavior)
-                    let &(_, attacker_class, _, attacker_is_melee, _, _) =
+                    let &(_, attacker_class, _, attacker_is_melee, _, _, attacker_kind) =
                         &combatant_info[&attacker_entity];
-                    let attack_range = if attacker_is_melee {
-                        MELEE_RANGE
-                    } else if attacker_class == match_config::CharacterClass::Hunter {
-                        AUTO_SHOT_RANGE
-                    } else {
-                        WAND_RANGE
+                    // AS-99 arm: range comes from the EQUIPPED weapon, not the
+                    // class. A live socket with no weapon in it (the Shaman's
+                    // relic) means no auto-attack at all.
+                    let attack_range = match attacker_kind {
+                        AutoAttackKind::Melee => MELEE_RANGE,
+                        AutoAttackKind::Shot => AUTO_SHOT_RANGE,
+                        AutoAttackKind::Wand => WAND_RANGE,
+                        AutoAttackKind::None => continue,
                     };
                     let distance = my_pos.distance(target_pos);
                     // Hunter dead zone: the ranged Auto Shot can't fire within 8
@@ -286,10 +309,7 @@ pub fn combat_auto_attack(
                     // (Spider/Boar) inherits the Hunter class but attacks in melee,
                     // so without the `!attacker_is_melee` guard it would skip every
                     // swing (it is always inside the dead zone while meleeing).
-                    if attacker_class == match_config::CharacterClass::Hunter
-                        && !attacker_is_melee
-                        && distance < HUNTER_DEAD_ZONE
-                    {
+                    if attacker_kind == AutoAttackKind::Shot && distance < HUNTER_DEAD_ZONE {
                         continue;
                     }
                     // Line-of-sight gate: ranged autos (Hunter Auto Shot,
@@ -301,7 +321,7 @@ pub fn combat_auto_attack(
                     // thin obstacle edge can still trade hits. Empty obstacle
                     // lists → always clear, so this is a byte-identical no-op on
                     // BasicArena / obstacle-free maps.
-                    if !attacker_is_melee
+                    if attacker_kind != AutoAttackKind::Melee
                         && !has_line_of_sight(&map_geometry.volumes, my_pos, target_pos)
                     {
                         continue;
@@ -517,7 +537,7 @@ pub fn combat_auto_attack(
                 {
                     // Look up the caster's team
                     if let Some(caster_entity) = aura.caster {
-                        if let Some(&(caster_team, _, _, _, _, _)) =
+                        if let Some(&(caster_team, _, _, _, _, _, _)) =
                             combatant_info.get(&caster_entity)
                         {
                             // Only track if the CC is from the opposing team of the target
@@ -549,7 +569,8 @@ pub fn combat_auto_attack(
         // Bug fix: Don't auto-attack targets with breakable CC from a friendly caster.
         // This prevents, e.g., a Warlock pet from breaking its team's Polymorph.
         if let Some(&cc_caster_team) = friendly_cc_team.get(&target_entity) {
-            if let Some(&(attacker_team, _, _, _, _, _)) = combatant_info.get(&attacker_entity) {
+            if let Some(&(attacker_team, _, _, _, _, _, _)) = combatant_info.get(&attacker_entity)
+            {
                 if attacker_team == cc_caster_team {
                     continue;
                 }
@@ -573,7 +594,7 @@ pub fn combat_auto_attack(
                 }
 
                 // Check for Frost Armor proc: if target has FrostArmorBuff and attacker is melee
-                if let Some(&(_, _, _, attacker_is_melee, _, _)) =
+                if let Some(&(_, _, _, attacker_is_melee, _, _, _)) =
                     combatant_info.get(&attacker_entity)
                 {
                     if attacker_is_melee {
@@ -602,7 +623,8 @@ pub fn combat_auto_attack(
                             target_auras.as_deref_mut(),
                         );
                         if fresh {
-                            if let Some((_, _, tname, _, _, _)) = combatant_info.get(&target_entity)
+                            if let Some((_, _, tname, _, _, _, _)) =
+                                combatant_info.get(&target_entity)
                             {
                                 combat_log.log(
                                     CombatLogEventType::CrowdControl,
@@ -633,14 +655,16 @@ pub fn combat_auto_attack(
                 // telegraphs a phantom release stroke. Inert in headless: like
                 // FloatingCombatText, the consuming systems live in
                 // rendering/effects.rs and are registered only in states/mod.rs.
-                if let Some(&(_, _, _, attacker_is_melee, _, _)) =
+                if let Some(&(_, _, _, _, _, _, attacker_kind)) =
                     combatant_info.get(&attacker_entity)
                 {
                     commands.spawn((
                         AutoAttackSwing {
                             attacker: attacker_entity,
                             target: target_entity,
-                            ranged: !attacker_is_melee,
+                            // AS-99 arm: the swing is ranged iff the equipped
+                            // weapon is a ranged one.
+                            ranged: attacker_kind != AutoAttackKind::Melee,
                         },
                         PlayMatchEntity,
                     ));
@@ -650,25 +674,30 @@ pub fn combat_auto_attack(
                 if let (
                     Some((
                         attacker_team,
-                        attacker_class,
+                        _attacker_class,
                         attacker_name,
-                        attacker_is_melee,
+                        _attacker_is_melee,
                         _,
                         attacker_slot,
+                        attacker_kind,
                     )),
-                    Some((target_team, _target_class, target_name, _, _, target_slot)),
+                    Some((target_team, _target_class, target_name, _, _, target_slot, _)),
                 ) = (
                     combatant_info.get(&attacker_entity),
                     combatant_info.get(&target_entity),
                 ) {
+                    // AS-99 arm: the log name is chosen by the same derived
+                    // kind as the range, so the two can never disagree.
                     let attack_name = if has_bonus {
                         "Heroic Strike" // Enhanced auto-attack
-                    } else if *attacker_is_melee {
-                        "Auto Attack"
-                    } else if *attacker_class == match_config::CharacterClass::Hunter {
-                        "Auto Shot"
                     } else {
-                        "Wand Shot"
+                        match attacker_kind {
+                            AutoAttackKind::Melee => "Auto Attack",
+                            AutoAttackKind::Shot => "Auto Shot",
+                            AutoAttackKind::Wand => "Wand Shot",
+                            // Unreachable: the range gate `continue`s on None.
+                            AutoAttackKind::None => "Auto Attack",
+                        }
                     };
                     let attacker_id = combat_log_id(*attacker_team, *attacker_slot, attacker_name);
                     let target_id = combat_log_id(*target_team, *target_slot, target_name);
diff --git a/src/states/play_match/components/combatant.rs b/src/states/play_match/components/combatant.rs
index e34395f..b4b9260 100644
--- a/src/states/play_match/components/combatant.rs
+++ b/src/states/play_match/components/combatant.rs
@@ -301,6 +301,9 @@ pub struct Combatant {
     pub slot: u8,
     /// Character class
     pub class: match_config::CharacterClass,
+    /// What this combatant's auto-attack IS — derived from the item actually
+    /// sitting in its live weapon socket, never from its class. AS-99 arm.
+    pub auto_attack_kind: AutoAttackKind,
     /// Resource type (Mana, Energy, Rage)
     pub resource_type: ResourceType,
     /// Maximum health points
@@ -441,8 +444,57 @@ pub fn weapon_poison_marker_aura(poison: RoguePoison) -> super::Aura {
     }
 }
 
-impl Combatant {
+/// AS-99 MEASUREMENT ARM — what a combatant's auto-attack IS.
+#[derive(Copy, Clone, Debug, PartialEq, Eq)]
+pub enum AutoAttackKind {
+    /// Swung in melee: `MELEE_RANGE`, logged "Auto Attack".
+    Melee,
+    /// Loosed from a bow/gun/crossbow: `AUTO_SHOT_RANGE`, dead zone, "Auto Shot".
+    Shot,
+    /// Fired from a wand: `WAND_RANGE`, logged "Wand Shot".
+    Wand,
+    /// No weapon in the live socket — no auto-attack at all.
+    None,
+}
+
+impl AutoAttackKind {
     /// Create a new combatant with class-specific stats.
+    ///
+    /// The kind of auto-attack a combatant has, derived from the EQUIPPED
+    /// weapon rather than from `CharacterClass::is_melee()`. `is_melee` answers
+    /// "how far away does this class swing"; it is asked four times in
+    /// `combat_core::auto_attack` (range gate, Hunter dead zone, LoS gate, the
+    /// `AutoAttackSwing.ranged` visual flag) plus once for the log name — and
+    /// none of those consult the socket holding the weapon.
+    pub fn from_class(class: match_config::CharacterClass) -> Self {
+        // The pre-AS-99 ladder, verbatim, so an un-equipped combatant behaves
+        // exactly as it does on main.
+        if class.is_melee() {
+            AutoAttackKind::Melee
+        } else if class == match_config::CharacterClass::Hunter {
+            AutoAttackKind::Shot
+        } else {
+            AutoAttackKind::Wand
+        }
+    }
+
+    /// The kind implied by an item in a given socket, or `None` when the item
+    /// is not a weapon at all (a relic, a shield, a held frill).
+    pub fn from_equipped(slot: ItemSlot, item: &super::super::equipment::ItemConfig) -> Self {
+        use super::super::equipment::WeaponType as W;
+        if !item.is_weapon {
+            return AutoAttackKind::None;
+        }
+        match (slot, item.weapon_type) {
+            (ItemSlot::Ranged, W::Bow | W::Gun | W::Crossbow | W::Thrown) => AutoAttackKind::Shot,
+            (ItemSlot::Ranged, W::Wand) => AutoAttackKind::Wand,
+            // A weapon in a hand socket is swung, whatever it is.
+            _ => AutoAttackKind::Melee,
+        }
+    }
+}
+
+impl Combatant {
     pub fn new(team: u8, slot: u8, class: match_config::CharacterClass) -> Self {
         // Base stats come from `class_base_stats` — the single table the UI
         // reads too, so a display can never drift from the sim.
@@ -468,6 +520,10 @@ impl Combatant {
             team,
             slot,
             class,
+            // Pre-equipment default = the old class ladder verbatim, so a bare
+            // combatant (unit tests, any spawn path that never equips) is
+            // unchanged. `apply_equipment` overwrites it from the live socket.
+            auto_attack_kind: AutoAttackKind::from_class(class),
             resource_type,
             max_health,
             current_health: max_health,
@@ -743,6 +799,11 @@ impl Combatant {
         // that answers attack RANGE, not which socket is live.
         let primary_weapon_slot = self.class.weapon_slot();
 
+        // AS-99 arm: the live socket decides what the auto-attack IS. Start
+        // from "no weapon" — an empty or relic-filled live socket must end
+        // here, and that is the case the class ladder cannot express.
+        self.auto_attack_kind = AutoAttackKind::None;
+
         for (slot, item_id) in loadout {
             let Some(item) = items.get(item_id) else {
                 continue;
@@ -764,6 +825,11 @@ impl Combatant {
             self.nature_resistance += item.nature_resistance;
             self.holy_resistance += item.holy_resistance;
 
+            // AS-99 arm: the live socket names the auto-attack, weapon or not.
+            if *slot == primary_weapon_slot {
+                self.auto_attack_kind = AutoAttackKind::from_equipped(*slot, item);
+            }
+
             // For the primary weapon slot, replace attack_damage and attack_speed
             if item.is_weapon && *slot == primary_weapon_slot {
                 let avg_damage = (item.attack_damage_min + item.attack_damage_max) / 2.0;
